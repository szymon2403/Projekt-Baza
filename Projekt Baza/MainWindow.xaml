<Window x:Class="Projekt_Baza.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekt_Baza"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListView Name="listapracownikow" MouseDoubleClick="listapracownikow_MouseDoubleClick">
            <ListView.View>
                <GridView AllowsColumnReorder="True">
                    <GridView.Columns>
                        <GridViewColumn Header="Id" DisplayMemberBinding="{Binding idpracownika}" />
                        <GridViewColumn Header="Imię" DisplayMemberBinding="{Binding imie}" />
                        <GridViewColumn Header="Nazwisko" DisplayMemberBinding="{Binding nazwisko}" />
                        <GridViewColumn Header="PESEL" DisplayMemberBinding="{Binding pesel}" />
                        <GridViewColumn Header="PESEL" DisplayMemberBinding="{Binding pesel}" />
                        
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
